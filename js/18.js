//Condicionales
const disponible = 4000
const quitar = 1000

if (disponible >= quitar ){//en el parentesis() ponemos la condicion que queremos comprobar
    console.log("Si puedes sacar")
}else{
    console.log("No tiene Saldo")
}

//Vamos a utilizar operadores
// > mayor que
// < menor que 
// >= mayor o igual que
// <= menor o igual que
// == igual 
// === igual estricto
// !== diferente a